# J5 — Confidence normalization and bounds

## Preconditions
- Any valid input object for `buildDecisionCard`

## Trigger
- Call `buildDecisionCard(input)` once.

## Expected (binary)
- `confidence >= 0.1`
- `confidence <= 0.95`
- confidence has two-decimal precision (`Number(confidence.toFixed(2)) === confidence`)

## Negative checks
- confidence is never `NaN`
- confidence is never outside configured min/max

## Timeout budget
- <= 50ms local execution.

# J2 — Playable-now path

## Preconditions
- Input has `hasDecklist=true`, `hasSideboardPlan=false`
- `gamesPlayed >= 8`, `winRate >= 0.45`
- `consistencyScore >= 0.5`, `rulesKnowledgeScore >= 0.55`
- `unresolvedBlockingIssues=[]`

## Trigger
- Call `buildDecisionCard(input)` once.

## Expected (binary)
- `state === "PLAYABLE_NOW"`
- `next_actions.length > 0`

## Negative checks
- `state !== "TOURNAMENT_READY"`
- `state !== "NOT_READY_YET"`

## Timeout budget
- <= 50ms local execution.

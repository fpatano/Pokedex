# J3 — Not-ready path with blockers

## Preconditions
- Input intentionally below playable thresholds (ex: decklist missing, low reps, low win rate)
- Optional unresolved blockers present

## Trigger
- Call `buildDecisionCard(input)` once.

## Expected (binary)
- `state === "NOT_READY_YET"`
- `blocking_issues.length > 0`
- `next_actions.length > 0`

## Negative checks
- `state !== "TOURNAMENT_READY"`
- `state !== "PLAYABLE_NOW"`

## Timeout budget
- <= 50ms local execution.

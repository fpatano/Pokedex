# J8 — Explainability consistency

## Preconditions
- Valid input that returns any decision state

## Trigger
- Call `buildDecisionCard(input)` once.

## Expected (binary)
- `explainability.reason_codes` exactly matches `top_reasons[].reason_code`
- `explainability.evidence_refs` exactly matches `top_reasons[].evidence_ref`
- `explainability.recommended_next_actions` exactly matches `next_actions`

## Negative checks
- no divergence between explainability arrays and surface response arrays

## Timeout budget
- <= 50ms local execution.

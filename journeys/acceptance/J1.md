# J1 — Tournament-ready happy path

## Preconditions
- Input has `hasDecklist=true`, `hasSideboardPlan=true`
- `gamesPlayed >= 25`, `winRate >= 0.55`
- `consistencyScore >= 0.75`, `rulesKnowledgeScore >= 0.8`
- `unresolvedBlockingIssues=[]`

## Trigger
- Call `buildDecisionCard(input)` once.

## Expected (binary)
- `state === "TOURNAMENT_READY"`
- `blocking_issues.length === 0`
- `next_actions.length > 0`

## Negative checks
- `state !== "PLAYABLE_NOW"`
- `state !== "NOT_READY_YET"`

## Timeout budget
- <= 50ms local execution.
